# ── Finnhub — Real-time quotes & news (REQUIRED) ──────────────────────────────
# Free tier: 60 req/min + WebSocket. Register at: https://finnhub.io
FINNHUB_API_KEY=d6rdp6pr01qr194ma61gd6rdp6pr01qr194ma620

# ── Groq — FREE AI (Llama 3.3 70B) — PRIMARY ─────────────────────────────────
# Completely free. 30 req/min, 14,400 req/day.
# Register at: https://console.groq.com → API Keys
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# ── OpenAI — Paid AI (optional secondary fallback) ────────────────────────────
# Only used if GROQ_API_KEY is empty. Requires credits.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ── MetaTrader 5 (optional — leave empty to run in mock/demo mode) ────────────
MT5_LOGIN=5436
MT5_PASSWORD=1Ss@hV3VGu
MT5_SERVER=PropXPLtd-Server
MT5_SYMBOL=NAS100

# ── Trading Symbol (Yahoo Finance format) ──────────────────────────────────────
# ^IXIC = NASDAQ Composite | ^NDX = NASDAQ 100 Index | QQQ = NASDAQ 100 ETF
SYMBOL=^NDX
CAPITAL=10000
RISK_PCT=1.0

# ── Server ────────────────────────────────────────────────────────────────────
HOST=0.0.0.0
PORT=8000
